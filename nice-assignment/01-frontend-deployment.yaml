apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app-deployment
  labels:
    app: my-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
        - name: frontend-app
          image: frontend-app:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
