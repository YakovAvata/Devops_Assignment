apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend-chart.fullname" . }}-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
  selector:
    app: {{ .Values.labels.app }}
